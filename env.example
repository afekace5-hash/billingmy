# Geniacs Configuration
GENIACS_API_KEY=your_api_key_here
GENIACS_SERVER_URL=https://api.geniacs.com/v1

# FreeRADIUS Database Configuration
# Required for syncing customer PPPoE accounts to RADIUS
# NOTE: The RADIUS_DB_HOST must have MySQL port (3306) accessible from this server
# If the RADIUS server is remote, you may need:
# 1. Open port 3306 in firewall (or use SSH tunnel)
# 2. Configure MySQL to allow remote connections (bind-address = 0.0.0.0)
# 3. Grant remote access: GRANT ALL ON radius.* TO 'radiususer'@'%' IDENTIFIED BY 'password';
# 
# For localhost RADIUS: use 127.0.0.1 or localhost
# For remote RADIUS with SSH tunnel: ssh -L 3306:localhost:3306 user@103.31.204.71
RADIUS_DB_HOST=localhost
RADIUS_DB_PORT=3306
RADIUS_DB_USER=radius
RADIUS_DB_PASS=radiuspassword
RADIUS_DB_NAME=radius

# ============================================================================
# PAYMENT GATEWAY CONFIGURATION
# ============================================================================

# Flip Payment Gateway
# Get credentials from: https://flip.id/dashboard
# Documentation: https://docs.flip.id
flip.secretKey=your_flip_secret_key_here
flip.validationToken=your_flip_validation_token_here
flip.environment=production
# Admin fees (in IDR)
flip.adminFeeVA=4000
flip.adminFeeQRIS=0
flip.adminFeeEwallet=0
flip.adminFeeRetail=2500

# Midtrans Payment Gateway (if using)
# midtrans.serverKey=your_midtrans_server_key
# midtrans.clientKey=your_midtrans_client_key
# midtrans.environment=production

# Duitku Payment Gateway (if using)
# duitku.merchantCode=your_merchant_code
# duitku.apiKey=your_api_key
# duitku.environment=production